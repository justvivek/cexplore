#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
	int i,num;
	scanf("%d",&num);
	int stud[num];
	i=0;
	while(i<num){
		scanf("%d\n", &stud[i]);
		++i;	
	}	
	for(int i=0; i<=num;i++){
	printf("%d\n",stud[i]);
	}
	return 0;
}
