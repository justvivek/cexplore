#include<stdio.h>
int main() {
/*
int a,b;
printf("Enter the first digit");
scanf("%d", &a);
printf("Enter the first digit");
scanf("%d", &b);

printf("Sum of two nos is: %d", a+b);
*/

char numa[100], numb[100];
int i=0,j=0,k=0;

printf("Enter the first number");
scanf("%s", numa);

printf("Enter the second number");
scanf("%s", numb);

while(numa[i]!='\0') i++;
while(numa[j]!='\0') j++;

for (i,j; i!=0 && j!=0; i--, j--)
{


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

}


return 0;
}

